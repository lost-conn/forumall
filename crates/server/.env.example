# Forumall Server Configuration
# Copy this file to .env and adjust values as needed.

# Server port (default: 8080)
PORT=8080

# Public base URL of the server - used for OFSCP discovery and federation
# Should match your actual deployment URL
FORUMALL_BASE_URL=http://localhost:8080

# Database storage path (default: aurora_db_data)
FORUMALL_DB_PATH=aurora_db_data

# Frontend serving mode: "proxy" | "static" | "disabled" (default: disabled)
# - proxy: Forward requests to a dev server (use with dx serve)
# - static: Serve pre-built files from a directory
# - disabled: API only, no frontend serving
FORUMALL_FRONTEND_MODE=disabled

# Target URL when FORUMALL_FRONTEND_MODE=proxy (default: http://localhost:8081)
FORUMALL_PROXY_TARGET=http://localhost:8081

# Static files directory when FORUMALL_FRONTEND_MODE=static (default: ./public)
FORUMALL_STATIC_DIR=./public

# Logging configuration (uses tracing-subscriber EnvFilter)
# Examples:
#   debug - All debug logs
#   forumall_server=debug,tower_http=debug - Debug for server and HTTP layer
#   info - Only info and above
RUST_LOG=forumall_server=debug,tower_http=debug
